set(AIAGENT_SRC)
set(AIAGENT_HDR)
set(AIAGENT_RES)
set(AIAGENT_UI)
set(AIAGENT_TRANS)

set(EDGEDEVICE_SRC)
set(EDGEDEVICE_HDR)
set(EDGEDEVICE_RES)
set(EDGEDEVICE_UI)
set(EDGEDEVICE_TRANS)

set(AIAGENT_BASE    "${CMAKE_CURRENT_LIST_DIR}/aiagent")
set(EDGEDEVICE_BASE "${CMAKE_CURRENT_LIST_DIR}/edgedevice")

include("${AIAGENT_BASE}/CMakeLists.txt")
include("${EDGEDEVICE_BASE}/CMakeLists.txt")
